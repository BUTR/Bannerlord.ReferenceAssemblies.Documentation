###############
#    folder   #
###############
/**/DROP/
/**/TEMP/
/**/packages/
/**/bin/
/**/obj/
_site

# Generated per build by build/generate-docfx-config.py from the packages
# that exist for the game version being built. The module set is not stable
# across versions, so a checked-in copy would be wrong for most of them.
docfx-meta.json
docfx-globals.json
api-delta.json
api/toc.yml
version.json
index.md
